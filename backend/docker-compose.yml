version: '3.8'

services:
  api:
    build: .
    container_name: diagnosis_backend
    ports:
      - "8000:8000"
    restart: always
    # volumes:
    #   - .:/app
    # environment:
    #   - ML_MODEL_PATH=/models/latest.bin

  # Заглушка для будущего ML-сервиса (если он будет вынесен в отдельный контейнер с GPU)
  # ml_worker:
  #   build: ./ml_service
  #   container_name: ml_worker
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]

  # Заглушка для базы данных (PostgreSQL)
  # db:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: hackathon_db
  #   ports:
  #     - "5432:5432"
